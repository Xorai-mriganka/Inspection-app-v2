// I am rewriting this in hooks because it will easy to understand and flexible to manage, also
// i think that this form is not properly written , it has way to many states which will make the 
// app rerender so many times 

import React from 'react'
import {useState,useEffect} from 'react'

const Step2_new = () => {
    const [engine, setEngine] = uEeState(initialState)
    return (
        <div>
            
        </div>
    )
}

export default Step2_new
